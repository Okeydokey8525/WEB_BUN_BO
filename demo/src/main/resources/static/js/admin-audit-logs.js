(() => {const form=document.querySelector('#audit-filter'),body=document.querySelector('#audit-body'),error=document.querySelector('#audit-error');let page=0;const query=()=>{const p=new URLSearchParams({page:String(page),size:'20'});const action=document.querySelector('#action').value.trim(),username=document.querySelector('#username').value.trim();if(action)p.set('action',action);if(username)p.set('username',username);return p;};async function load(){error.textContent='';try{const r=await fetch(`/api/admin/audit-logs?${query()}`);if(!r.ok)throw new Error(`Không thể tải nhật ký (${r.status})`);const data=await r.json();body.replaceChildren();(data.content||[]).forEach(log=>{const tr=document.createElement('tr');[log.timestamp,log.username,log.action,`${log.entityType||''} ${log.entityId||''}`,log.metadata||''].forEach(v=>{const td=document.createElement('td');td.textContent=v;tr.append(td);});body.append(tr);});if(!body.children.length){const td=document.createElement('td');td.colSpan=5;td.textContent='Không có nhật ký';body.append(Object.assign(document.createElement('tr'),{appendChild:()=>td}));}}catch(e){error.textContent=e.message;}}form.addEventListener('submit',e=>{e.preventDefault();page=0;load();});load();})();
